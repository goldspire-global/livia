appId: host.exp.exponent
---
# Optional demo sign-in — skip when already authenticated (caller must have app open)
- runFlow:
    when:
      visible:
        id: "email-input"
    file: sign-in-demo-steps.yaml
