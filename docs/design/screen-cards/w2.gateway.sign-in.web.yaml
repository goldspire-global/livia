meta:
  id: w2.gateway.sign-in.web
  world: W2
  route: "/sign-in"
  artifact: livia-dashboard
  persona: [All tenant users]
  release: R1
  status: designed

job: >
  Clerk sign-in — gateway aurora W2 — Liv colleague story left, auth right; routes to correct persona home.

visual:
  canvas:
    device: responsive
    viewport: 390x844 minimum
    background: aurora gateway gradient — split layout desktop
  zones:
    - id: brand_panel
      visible: desktop left ~50%
      content:
        - Livia mark + eyebrow "Your people-business OS"
        - headline "Tuesday morning, handled."
        - subcopy one line — continuity / colleague framing (not salon-shaped)
        - Liv briefing card — calm operator tone, 2–3 lines + soft CTA chip
    - id: clerk_panel
      content:
        - Clerk SignIn component themed cyan primary ADR
        - email + password / magic link
        - link to sign-up
      max_width: 400px centered
  motion:
    enter: fade 300ms
  northstarRealPath: docs/design/assets/w2-gateway/sign-in/gateway-default.target.png

states:
  error_auth:
    clerk: inline errors — no custom stack traces

acceptance:
  e2e:
    - demo login flows DEMO-LOGINS.md
  visual:
    - baseline: docs/design/assets/w2-gateway/sign-in/gateway-default.target.png

traceability:
  surfaceId: gateway.sign-in
  figma:
    frame: W2/w2.gateway.sign-in.web
    status: not_started
